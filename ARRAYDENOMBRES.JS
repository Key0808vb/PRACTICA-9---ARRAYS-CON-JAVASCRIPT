// Ejercicio 1: Crear un array de nombres y mostrar el primero y el último.

const nombres = ["Ana", "Luis", "María", "Carlos", "Sofía"];

const primero = nombres[0];

const ultimo = nombres[nombres.length - 1];

console.log("El primer nombre es:", primero);
console.log("El último nombre es:", ultimo);

El primer nombre es: Ana
El último nombre es: Sofía
